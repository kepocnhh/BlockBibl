package stan.block.note;public class Note //Запись	extends Element{//Поля    Part[] parts; //элементы записи    int font_size; //размер шрифта по умолчанию    String font; //шрифт по умолчанию    String color_text; //цвет текста по умолчанию    String color_back; //цвет фона по умолчанию//Методы    void add_part(Part p)    {        Part[] tmp = parts.clone();        parts = new Part[tmp.length+1];        for(int i=0;i<tmp.length;i++)        {            parts[i] = tmp[i];        }        parts[tmp.length] = p;    }    void rem_part()    {        Part[] tmp = parts.clone();        parts = new Part[tmp.length-1];        for(int i=0;i<tmp.length-1;i++)        {            parts[i] = tmp[i];        }    }//Конструкторы    Note(String s)    {        name=s;    }}