package stan.block.note;public class Block //Блок записей	extends Element{//Поля    public Element[] elements; //элементы блока//Методы    public void add_element(Element e)    {        Element[] tmp = elements.clone();        elements = new Element[tmp.length+1];        for(int i=0;i<tmp.length;i++)        {            elements[i] = tmp[i];        }        elements[tmp.length] = e;    }    public void rem_element()    {        Element[] tmp = elements.clone();        elements = new Element[tmp.length-1];        for(int i=0;i<tmp.length-1;i++)        {            elements[i] = tmp[i];        }	    }//Конструкторы	public    public Block(String s)    {        name=s;    }}